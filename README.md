# Chrisnerg Mqobo — Portfolio (React + Vite + Tailwind)

## Setup

```bash
npm install
npm run dev
```

Then open http://localhost:5173

## Build for Production

```bash
npm run build
npm run preview
```

## Notes
- Profile photo is embedded as base64 in `src/photoData.js`
- CV PDF is in `public/Junior_software_engineer.pdf`
- Design matches original HTML exactly — same fonts, colors, animations, layout
